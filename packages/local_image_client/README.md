# local_image_client

Utility for persisting image bytes on disk and tracking simple metadata for
local caching scenarios. Combine it with your preferred HTTP client in a
repository to download the bytes before handing them to the storage layer.
